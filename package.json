{
  "name": "hushfern",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "type": "module",
  "scripts": {
    "prepare:model": "node scripts/fetch-model.mjs",
    "dev": "yarn prepare:model && vite build --watch",
    "build": "yarn prepare:model && vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.5",
    "sass": "^1.102.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
